#include <stdio.h>

int minutos (int hour, int min) {
    int total = 0;
    total = (hour * 60);
    total = min;
    return total;
}

int diferenca (int total1, int total2) {
    return total1 + total2;
}

int main() {
    
}